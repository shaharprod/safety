PORT=4000
DATABASE_URL=postgresql://user:password@localhost:5432/safetyos
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=yourapppassword
# Where project-manager → safety-officer coordination updates are emailed.
# Falls back to SMTP_USER if unset.
SAFETY_OFFICER_EMAIL=safety.officer@email.com
# Public app URL used in email action links (falls back to request host)
APP_URL=http://localhost:5173
NODE_ENV=development
